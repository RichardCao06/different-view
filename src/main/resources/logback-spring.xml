<?xml version="1.0" encoding="UTF-8"?>
<!-- TRACE < DEBUG < INFO < WARN < ERROR，ERROR最高级 -->
<!--true：配置文件如果更改就会重新加载，60 seconds设置时间间隔60s，debug：当此属性设置为true时，将打印出logback内部日志信息-->
<configuration scan="true" scanPeriod="60 seconds" debug="false">
    <!-- 定义日志的根目录 -->
    <property name="LOG_HOME" value="/log"/>
    
    <!--输出到控制台-->
    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <!--日志输出格式：%d表示日期时间，%thread表示线程名，%-5level：级别从左显示5个字符宽度，%logger{50} 表示logger名字最长50个字符，否则按照句点分割，%msg：日志消息，%n是换行符-->
        <layout class="ch.qos.logback.classic.PatternLayout">
            <!--开发环境-->
            <springProfile name="dev">
                <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} ---> [%thread] ---> %-5level %logger{50} - %msg%n</pattern>
            </springProfile>
            <!--非开发环境-->
            <springProfile name="!dev">
                <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} ==== [%thread] ==== %-5level %logger{50} - %msg%n</pattern>
            </springProfile>
        </layout>
        <!--过滤方式：只记录INFO级别以上的日志-->
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>info</level>
        </filter>
    </appender>
    
    <!--打印ERROR日志到文件中-->
    <appender name="fileErrorLog" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <!--滚动策略-->
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <FileNamePattern>${LOG_HOME}/FileErrorLog/errorLog(%d{yyyy-MM-dd}).log</FileNamePattern>
        </rollingPolicy>
        <layout>
            <pattern>[%d{yyyy-MM-dd HH:mm:ss}] [%-5level] [%logger:%line] --%mdc{client} %msg%n</pattern>
        </layout>
        <!--过滤方式：只记录ERROR级别的日志-->
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
    </appender>
    
    <!--打印WARN以及ERROR日志到文件中-->
    <appender name="fileWarnLog" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <!--滚动策略-->
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <FileNamePattern>${LOG_HOME}/FileWarnLog/warnLog(%d{yyyy-MM-dd}).log</FileNamePattern>
        </rollingPolicy>
        <layout>
            <pattern>[%d{yyyy-MM-dd HH:mm:ss}] [%-5level] [%logger:%line] --%mdc{client} %msg%n</pattern>
        </layout>
        <!--过滤方式：只记录WARN级别以上的日志-->
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>WARN</level>
        </filter>
    </appender>

    <!--分别设置对应的日志输出节点-->
    <root level="INFO">
        <!--将日志输出到控制台中-->
        <appender-ref ref="console"/>
        <!--打印ERROR日志-->
        <appender-ref ref="fileErrorLog" />
        <!--打印WARN日志-->
        <appender-ref ref="fileWarnLog"/>
    </root>
</configuration>